version: 1.0
runtime: python3
build:
  commands:
    build:                                 
      - pip install --no-cache-dir -r requirements.txt
run:
  command: uvicorn server:app --host 0.0.0.0 --port 8080
